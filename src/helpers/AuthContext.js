import { createContext } from "react";

export const AuthContext = createContext("");       ///has all the information of user and also chacks whether the user is 
                                                    //authenticated or not